/* ===================================================================
   앱 버전 — 배포할 때마다 끝자리를 올린다 (1.1.1 → 1.1.2 → …).
   ★ 끝자리가 9 를 넘으면 자리를 올린다: 1.1.9 → 1.2.0 → 1.2.1 → …
     (semver 대로면 1.1.10 이 맞지만, 버전을 눈으로 보고 최신본인지 가리기 때문에
      자리를 넘기는 쪽으로 정했다 — 2026-08-04)
   ※ 2026-08-04 에 6.4.2 에서 1.1.1 로 다시 시작했다. 번호만 새로 매긴 것이고
     기능은 그대로다. 서비스워커 캐시 이름도 이 값을 따라가므로,
     내려간 번호 때문에 캐시가 꼬이지 않도록 이름이 달라지기만 하면 된다.
   여기 한 줄만 고치면 화면 배지·푸터·서비스워커 캐시 이름이 전부 따라온다.
   ※ 회사별 설정(qo-config.js)이 아니라 공용 파일이다. 배포 때 같이 올린다.
   =================================================================== */
var APP_VER = "2.0.3";

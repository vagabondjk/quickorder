/* ===================================================================
   앱 버전 — 배포할 때마다 끝자리를 올린다 (6.2.7 → 6.2.8 → …).
   ★ 끝자리가 9 를 넘으면 자리를 올린다: 6.2.9 → 6.3.0 → 6.3.1 → …
     (semver 대로면 6.2.10 이 맞지만, 버전을 눈으로 보고 최신본인지 가리기 때문에
      자리를 넘기는 쪽으로 정했다 — 2026-08-04)
   여기 한 줄만 고치면 화면 배지·푸터·서비스워커 캐시 이름이 전부 따라온다.
   ※ 회사별 설정(qo-config.js)이 아니라 공용 파일이다. 배포 때 같이 올린다.
   =================================================================== */
var APP_VER = "6.3.9";
